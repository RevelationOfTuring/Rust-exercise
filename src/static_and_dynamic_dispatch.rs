/*
    如何用Rust实现多态？
    Rust 可以同时支持“静态分派”(static dispatch)和“动态分派”(dynamic dispatch)。

    静态分派：指具体调用哪个函数，在编译阶段就确定下来了 。
    Rust中的“静态分派”靠泛型以及 imp! trait来完成 。
*/

#[cfg(test)]
mod tests {
    #[test]
    fn test_static_and_dynamic_dispatch() {

    }
}